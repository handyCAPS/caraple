export enum EDBKeys {
  db = 'caraple',
  games = 'games',
  users = 'users',
}

export enum EGamesDBKey {
  word = 'word',
  guesses = 'guesses',
  timeSpent = 'timeSpent',
  date = 'date',
  user = 'user',
}
