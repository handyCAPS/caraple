import { DateValuesPipe } from './date-values.pipe';

describe('DateValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new DateValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
